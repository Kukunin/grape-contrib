require "spec_helper"

RSpec.describe Grape::Contrib do
  it "has a version number" do
    expect(Grape::Contrib::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
